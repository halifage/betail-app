# farmOS host (without https://) — pre-fills the Login screen Farm URL field.
VITE_FARMOS_HOST=farm.betail.app

# OAuth client ID registered in farmOS via betail-infra scripts/create-oauth-client.sh
VITE_OAUTH_CLIENT_ID=betail-pwa
